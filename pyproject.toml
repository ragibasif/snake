[project]
name = "snake"
version = "1.0.0"
description = "Snake is a lightweight CLI utility that sanitizes file and directory names by replacing non-alphanumeric characters with underscores."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
